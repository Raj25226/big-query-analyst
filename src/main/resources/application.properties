# ── Server ─────────────────────────────────────────────────────────────────
server.port=8000
spring.application.name=bq-agent-looker

# ── GCP Configuration ─────────────────────────────────────────────────────
# Required: your GCP project ID
gcp.project.id=${GCP_PROJECT_ID:aesthetic-kite-456610-b9}

# Optional: path to service account key JSON file
# If blank, Application Default Credentials (ADC) will be used
gcp.sa.key.path=${GCP_SA_KEY_PATH:}
